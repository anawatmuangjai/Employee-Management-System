@model SkillGroupViewModel

@{
    ViewData["Title"] = "Index";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3">
        <h1 class="h2">Skill Group Configuration</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group mr-2">
                <a class="btn btn-sm btn-outline-secondary" asp-action="Create">Create</a>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col">
            @if (Model.SkillGroups.Any())
            {
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Skill Group</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.SkillGroups)
                        {

                            <tr>
                                <td>@item.SkillGroupId</td>
                                <td>@item.SkillGroupName</td>
                                <td>
                                    <a asp-controller="SkillGroup" asp-action="Edit" asp-route-id="@item.SkillGroupId" class="btn btn-info btn-sm">Edit</a>
                                    <a asp-controller="SkillGroup" asp-action="Delete" asp-route-id="@item.SkillGroupId" class="btn btn-danger btn-sm">Delete</a>
                                </td>
                            </tr>

                        }
                    </tbody>
                </table>
            }
        </div>
    </div>
</div>


